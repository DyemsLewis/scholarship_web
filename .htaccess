<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hostinger Web and Cloud plans use public_html as a fixed document root.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

<FilesMatch "^\.">
    Require all denied
</FilesMatch>
